# InnerLens
Build the Reflect XR mobile app (required) and add InnerLens as a standout feature — an AI chatbot that converses with the user, detects their emotional state, and automatically generates art based on how they're feeling, without the user needing to craft prompts manually.
